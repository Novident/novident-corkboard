import 'package:flutter/material.dart';

class NovidentCorkboard extends StatelessWidget {
  const NovidentCorkboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
